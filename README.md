# Functions for Buget Jukov 2026

Commands:

- npm run serve — start emulators (functions + hosting)
- npm run import-csv — import CSV file into Firestore (collection `imported_2026`)
- npm run deploy — deploy functions and hosting to Firebase (project bugetjukov-2026)
