rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow authenticated (incl. anonymous) users to read/write budgets
    match /budgets/{budgetId} {
      allow read, write: if request.auth != null;
    }
  }
}


